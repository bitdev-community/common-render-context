---
labels: ['module', 'webpack', 'transformer']
description: 'Render context that wraps the components with a border'
---

This is a a render context that wraps the components with a border. The render context is used by both the Env and Test Runner.
